{% extends 'base.html.twig' %}

{% block title %}Log in!{% endblock %}

{% block body %}
    <div class="container-fluid">
        <div class="row">
            <div class="col"></div>
            <div class="col-6">
                {% if error %}
                    <div class="alert alert-danger">
                        <div class="text-center">
                            {{ error.messageKey|trans(error.messageData, 'security') }}
                        </div>
                    </div>
                {% endif %}
            </div>
            <div class="col"></div>

            {% if app.user %}
                <div class="mb-3">
                    You are logged in as {{ app.user.username }}, <a href="{{ path('app_logout') }}">Logout</a>
                </div>
            {% endif %}
        </div>
    </div>
    <div class="container-fluid" style="padding-top: 15%">
{#        <div class="row">#}
{#            <div class="col"></div>#}
{#            <div class="col-4">#}
                <div class="style_inner">
                    <form method="post" class="form-sign-in">
{#                        <div class="form-group">#}
                            <h1 class="h3 mb-3 font-weight-normal text-center" style="text-shadow: 1px 1px 15px #000000">Добро пожаловать!</h1>
{#                            <div class="form-group">#}
                                <label for="inputPhone" class="sr-only">Phone number</label>
                                <input type="text" value="{{ last_username }}" name="phone" id="inputPhone"
                                       class="form-control" placeholder="Phone number"
                                       required
                                       autofocus>
{#                            </div>#}

{#                            <div class="form-group">#}
                                <label for="inputPassword" class="sr-only">Пароль</label>
                                <input type="password" name="password" id="inputPassword" placeholder="Password" class="form-control" required>

                                <input type="hidden" name="_csrf_token"
                                       value="{{ csrf_token('authenticate') }}"
                                >
{#                            </div>#}



                            {#
                                Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
                                See https://symfony.com/doc/current/security/remember_me.html

                                <div class="checkbox mb-3">
                                    <label>
                                        <input type="checkbox" name="_remember_me"> Remember me
                                    </label>
                                </div>
                            #}

                            <button class="btn btn-lg btn-primary btn-block" type="submit">
                                Войти
                            </button>
{#                        </div>#}
                    </form>

{#                </div>#}

            </div>
{#            <div class="col"></div>#}
{#        </div>#}

    </div>
{% endblock %}